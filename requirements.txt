Flask==2.0.3          # Use Flask version compatible with Werkzeug
Werkzeug==2.0.3       # Use compatible version with Flask
SQLAlchemy==1.4.36    # ORM for database interactions
Celery==5.2.3         # Task scheduling and background jobs
redis==4.1.0          # Redis as the broker for Celery
flask-restful==0.3.9  # RESTful extension for Flask
